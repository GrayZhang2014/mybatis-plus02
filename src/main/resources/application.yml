# 配置数据源
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp?serverTimezone=Asia/Shanghai&zone=useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: '123456'
    
# 配置日志
logging:
  level:
    root: warn
    com.mp.dao: trace
  pattern:
    console: '%p%m%n'